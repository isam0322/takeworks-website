# 方向宣言: TAKE WORKS 公式サイト

2026-08-26 制定。以後このサイトの全UI作業はこの宣言を正典とする。
**既存の方向（ダーク×ゴールド／静謐×精密）は変更しない。** 実行精度だけを上げる。
宣言から外れたくなったら、先にこの文書を更新してから外れる。

---

## 基本

- **ジョブ**: 「好きなものはあるが、それで食えるとは思っていない」人に、作って外に出している実物を見せ、公式LINEに繋ぐ
- **レジスター**: 主=静謐（漆黒＋金属色レーン） 副=精密（整列とグリッドの規律としてのみ）
  - 静謐＝招く。売り込まない。余白が格を語る
  - 精密＝1pxまで揃っていること自体が信用になる。ダッシュボード的な密度の話ではない
- **サーフェス**: web（1ページのブランドサイト）

## パレット（実HEX・これ以外を足さない）

| 役割 | HEX | 用途 |
|---|---|---|
| 地 | `#0a0a0a` | ページ全体の地 |
| 面 | `#131313` | セクションの面・カード |
| 面（浮き） | `#1a1a1a` | hover時のカード |
| 文字 | `#e8e8e8` | 本文・見出し |
| 副文字 | `#888888` | 補足・メタ（地に対して5.58:1・AA合格） |
| アクセント | `#c8a96e` | CTA・見出しの決め所・罫線のみ |

- **`--text-dim` (#555555) は廃止。** 地に対して2.66:1でWCAG AA不合格。副文字は必ず `--text-muted` (#888888) を使う
- 黒に近い色を増やさない。地・面・面（浮き）の3段だけで階層を作る
- 金は「グラデーションのスープ」にしない。金グラデ文字は静謐の禁じ手

## タイポ

- display / body = **Noto Sans JP**、英字ラベル・数字 = **Space Grotesk**
- `font-family` は**欧文を先、和文を後**（`'Space Grotesk', 'Noto Sans JP'`）。和文フォントの従属欧文は品質が低く、「消防士15年 × DJ10年」のような和欧混植で数字が沈む
- 本文行間 **1.8**（和文は欧文の1.5では窮屈）
- 見出し行間 **1.4**
- **本文の行長は全角40字まで**（`max-width: 40em` 相当）。超えると目が戻れなくなる
- `font-feature-settings: "palt"` は**見出しだけ**。本文に効かせるとリズムがガタつく

## 揃え方（今回の最大の修正点）

- **和文の本文は左揃え。** 中央揃えは「ラベル・見出し・単独のCTA」に限る
  - 理由: 4段落の日本語を中央揃えにすると行の左右がガタガタになり、これが「素人っぽさ」の最大の発生源になっていた
- セクション見出しとラベルは中央のままでよい（型として成立している）

## 余白リズム

- **8ptグリッド厳守**。使ってよい値は `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 120` のみ
- 9px・7px・3px・14px・18px・28px・34px・36px・56px のような中途半端な値を新たに書かない
- セクション間 120px（モバイル 80px）／要素間 24px または 32px

## 角丸

- **3種だけ**: `--radius-sm: 4px`（タグ・小さなラベル） / `--radius: 12px`（カード・ボタン） / `999px`（ピル）
- 20px・8px・2px・100px は使わない（同じ意味の値を二重に持たない）

## モーション

- 基準 **180ms ease-out**（`--ease` のカーブを維持）
- **`transition: all` を禁止**（`transform` と `opacity` のみ指定する）。レイアウトを揺らすプロパティのアニメは重い
- バウンス禁止（静謐は臨界減衰）。500ms を超えない
- `prefers-reduced-motion: reduce` 対応は維持

## シグネチャー（製品固有の見せ場・1点だけ）

**WORKS をレコードのトラックリストとして組む。**

- 各作品に `01` `02` `03` … の通し番号を Space Grotesk・`tabular-nums` で打ち、金の細い罫線で区切る
- 盤の裏ジャケの曲目表の佇まい。DJ・作品を出す人間のサイトであることが、装飾を足さずに伝わる
- 番号は主張しすぎない（副文字色・小さめ）。**光るのは罫線と番号の整列であって、色ではない**

これ以外の「見せ場」を足さない。静謐は足し算で壊れる。

## 禁じ手（このレジスターで出やすいスロップ）

1. **中央揃えの長文** — 今回の主犯。本文を中央に置かない
2. **カードの乱発** — 何でもかんでも枠で囲むと、全部が同じ重さになり主役が消える
3. **金の増量** — 効かないからと金を足すのは逆効果。金は「1画面に1つ際立たせる」ためだけに使う（Von Restorff）
4. カウントダウン・「残りわずか」等の焦らし（静謐の禁じ手であり、景表法上も実態のない限定表示は不可）

## 品質の床（実装後に必ず確認）

- コントラスト 4.5:1 以上（副文字も含む）
- タップターゲット 44×44px 以上（モバイル）
- `:focus-visible` が全リンク・ボタンで見える
- 375px 幅で横スクロールが出ない
- 和文本文で行間1.8未満・行長40字超がない
